import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AuditReportsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Audit Reports</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the audit reports page.</p>
      </CardContent>
    </Card>
  );
}
