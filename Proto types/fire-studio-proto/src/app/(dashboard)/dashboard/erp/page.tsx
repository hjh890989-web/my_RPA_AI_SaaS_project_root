import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ErpPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ERP Integration</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the ERP dashboard page.</p>
      </CardContent>
    </Card>
  );
}
