import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function SecurityPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Security Console</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the security console page.</p>
      </CardContent>
    </Card>
  );
}
