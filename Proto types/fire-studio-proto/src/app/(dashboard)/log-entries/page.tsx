import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function LogEntriesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Data Logging</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the data logging page.</p>
      </CardContent>
    </Card>
  );
}
