import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function LogReviewPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Log Review (HITL)</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the log review page.</p>
      </CardContent>
    </Card>
  );
}
